#ifndef PLUGIN_H
#define PLUGIN_H

typedef struct modexception {
	
	unsigned int code;
	const char *msg;

} mod_t;

struct fooarg {
	/* 
	 * 
	 * nic
	 * ip
	 * netmask
	 * optarg
	 * 
	 */
};


#endif
